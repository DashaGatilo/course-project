# Используем официальный образ MySQL 8.3
FROM mysql:8.3

# Копируем дамп-файлы в контейнер
# Предполагается, что дамп-файлы находятся в папке "dumps" рядом с Dockerfile
COPY ./dumps /docker-entrypoint-initdb.d/

# Дамп-файлы будут автоматически накатываться при старте контейнера благодаря скриптам в папке /docker-entrypoint-initdb.d
# MySQL запускает все скрипты *.sql, *.sh и *.sql.gz в этой папке при первом старте.

# Порты, которые будут открыты для доступа
EXPOSE 3306

# Завершаем создание Dockerfile
CMD ["mysqld"]
